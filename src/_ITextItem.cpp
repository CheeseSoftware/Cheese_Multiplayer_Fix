/*#ifndef _SERVER
#include "TextItem.h"
#include <SFML\Graphics.hpp>

TextItem::TextItem(sf::Text)
{
	this->text = text;
	//text.setPosition(x + 1, y + height - text.getCharacterSize()<<1);
}

void TextItem::Draw(App &app, int x, int y)
{
	app.draw(text);
}
#endif*/